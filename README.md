docker khá giống git
docker sẽ lấy các image từ docker hub
docker là stateless

các lệnh đã dùng:
  - docker run -it --entrypoint=bash ubuntu(image) 
  - docker ps -a
  - docker ps -aq

- uv là môi env manager trong python
- to_parquet use pyarrow
- when u want to re-content the commit you can: git commit -amend -m "msg"
- if you push on remote github you can change by: git push --force